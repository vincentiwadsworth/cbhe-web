---
interface Commodity {
  price: number;
  unit: string;
  label: string;
}

interface PricesData {
  updated: string;
  usd_bob: { official: number };
  commodities: {
    WTI: Commodity;
    BRENT: Commodity;
    HENRY_HUB: Commodity;
    TTF: Commodity;
  };
}

interface Props {
  data: PricesData;
}

const { data } = Astro.props;
const { commodities, usd_bob } = data;

const updatedDate = new Date(data.updated);
const fmtDate = new Intl.DateTimeFormat("es-BO", {
  day: "2-digit",
  month: "2-digit",
  year: "2-digit",
  hour: "2-digit",
  minute: "2-digit",
  hour12: false,
  timeZone: "America/La_Paz",
});

const fmtPrice = (p: number) => p.toFixed(2);

const items = [
  commodities.WTI,
  commodities.BRENT,
  commodities.HENRY_HUB,
  commodities.TTF,
];
---

<section class="bg-inverse-surface py-4 sm:py-6">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 md:px-8">
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4 sm:gap-6">
      {items.map((item) => (
        <div class="p-4 flex flex-col items-center text-center">
          <p class="text-inverse-on-surface/70 text-xs uppercase tracking-wide">{item.label}</p>
          <p class="text-inverse-on-surface font-extrabold text-xl sm:text-2xl lg:text-3xl tabular-nums tracking-tight">
            {fmtPrice(item.price)}
          </p>
          <p class="text-inverse-on-surface/70 text-xs sm:text-sm">{item.unit}</p>
        </div>
      ))}

      <div class="p-4 flex flex-col items-center text-center">
        <p class="text-inverse-on-surface/70 text-xs uppercase tracking-wide">Dólar oficial BCB</p>
        <p class="text-inverse-on-surface font-extrabold text-xl sm:text-2xl lg:text-3xl tabular-nums tracking-tight">
          {fmtPrice(usd_bob.official)}
        </p>
        <p class="text-inverse-on-surface/70 text-xs sm:text-sm">Bs/USD</p>
      </div>
    </div>

    <p class="text-inverse-on-surface/70 text-xs text-center mt-4 sm:mt-6">
      Actualizado {fmtDate.format(updatedDate)}
    </p>
  </div>
</section>
